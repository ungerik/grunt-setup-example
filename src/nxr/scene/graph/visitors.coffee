nxr.scene.graph.setupScene = (scene) ->

nxr.scene.graph.preRender = (scene) ->

nxr.scene.graph.render = (scene) ->

nxr.scene.graph.postRender = (scene) ->

nxr.scene.graph.teardownScene = (scene) ->